describe('TodoCardComponent is trivial, so we do not actually test it', () => {
    /*
     * The `TodoCardComponent` has no actual logic in
     * it, so there's really nothing to test here.
     */
});
